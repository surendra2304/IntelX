# IntelX Engineering Diary — 2026-08-28

## Daily Summary
- I initialized the core IntelX project architecture and GitHub repository infrastructure.
- I formulated strict file length and bullet count constraints for daily logs and summary indices.
- I authored an automated verification script to validate diary invariants and formatting rules.
- I built the complete foundational application stack using Python 3.11, FastAPI, and Uvicorn.
- I implemented asynchronous database support via SQLAlchemy 2.0 and aiosqlite with SQLite WAL mode.
- I engineered the comprehensive evidence data model spanning all 18 typed relational database tables.
- I authored Alembic migrations establishing relational schemas and SQLite FTS5 full-text search indexes.
- I developed typed repository layer in `intelx/db/repos.py` with span integrity and cryptographic audit chaining.
- I engineered the central model gateway in `intelx/models/gateway.py` supporting role-based LLM routing.
- I built the offline MockProvider with deterministic role factories and structured output self-correction.
- I engineered HttpFetchConnector with mandatory SSRF protection, IP resolution checks, and redirect guards.
- I built WebSearchConnector and FileConnector supporting PDF, DOCX, HTML, Markdown, CSV, and JSON parsing.
- I developed the text normalization and chunking pipeline with verified absolute character offsets.
- I built the prompt injection scanner flagging risks into sidecar JSON files without mutating content.
- I developed BaseAgent architecture and AgentRegistry enforcing user-message document delimiter contracts.
- I built PlannerAgent, ScoutAgent, RetrieverAgent, and ExtractorAgent with verbatim span enforcement.
- I engineered VerifierAgent with 3-gram Jaccard independence rules and quarantine tier verdict caps.
- I implemented the deterministic v1 composite confidence scoring formula with penalty deduction tracking.
- I developed EntityResolver with corporate alias indexing, automated high-confidence merges, and audit logging.
- I created AnalystAgent and CriticAgent performing timeline derivation, graph extraction, and draft critiques.
- I built OrchestrationEngine DAG state machine with concurrency caps, budget ceilings, and review gates.
- I engineered machine-enforced citation integrity validation resolving `[S:id]` and `[C:id]` tokens in `core/report.py`.
- I created the atomic multi-format artifact manager producing `report.md`, `report.json`, `evidence_pack.json`, and `sources.csv`.
- I engineered the complete API v1 surface in `intelx/api/v1/` with Bearer auth, rate limiting, and RFC 7807 errors.
- I built server-rendered web workspace with Jinja2 templates, vanilla JS, and hand-written offline CSS.
- I engineered global regex log scrubber redacting bearer tokens, API keys, and sensitive credentials across all logs.
- I created the 8-task golden evaluation harness in `evals/` benchmarking contradiction recall and citation integrity.
- I developed the concurrency smoke test suite verifying zero state bleed across 5 simultaneous mock pipelines.
- I authored comprehensive test suites achieving 100% green pass rate across 84 automated tests.

## What I Built & Did
- **Evaluation Harness & Test Hardening (Step 12)**:
  - I created 8 golden evaluation tasks (`evals/golden/*.json`) and realistic test documents in `evals/fixtures/`.
  - I authored `evals/run.py` deterministic evaluation harness evaluating citation validity, groundedness, and contradictions.
  - I established `evals/thresholds.json` quality gates and generated baseline `evals/results.json` (100% quality scores).
  - I authored `tests/test_concurrency.py` verifying state isolation, isolated cost accounting, and zero claim bleed across 5 parallel runs.
  - I added `make eval` target and documented baseline evaluation metrics table in `README.md`.
- **Testing & Verification**:
  - I ran pytest test suites achieving 84 passed tests (100% pass rate).

## Bugs I Found & Fixed
- **SQLite Concurrency Lock Contention**:
  - *Bug*: Concurrent async pipeline flushes hit `database is locked` on SQLite under parallel write loads.
  - *Fix*: Increased SQLite `busy_timeout` to 60000ms and set driver `connect_args={"timeout": 60.0, "check_same_thread": False}`.
- **Evaluation Harness Artifact Query**:
  - *Bug*: `evals/run.py` attempted to import non-existent `ArtifactManager` helper.
  - *Fix*: Replaced with direct SQLAlchemy `select(Artifact)` queries filtering on `ArtifactFormat.MD`.

## Key Decisions & Architecture
- **Mechanical Quality Gates**: Evaluated groundedness, citation validity, and independence through deterministic assertions rather than probabilistic scoring.
- **Deterministic Baseline**: Committed `evals/results.json` as permanent benchmark for regression detection across future model changes.

## Testing, Security & State
- **Automated Tests**: 84 passed (100% pass rate across concurrency, adversarial, web UI, API, artifacts, engine, worker, trust, agents, connectors, DB).
- **Evaluation Gate**: 100% citation validity, 100% groundedness, 100% contradiction recall, 100% null result correctness.
- **Git Commits**: Step 1 through Step 12 complete, fully tested, and ready to push to remote master branch.
