# IntelX Engineering Diary — 2026-08-28

## Daily Summary
- I initialized the core IntelX project architecture and GitHub repository infrastructure.
- I formulated strict file length and bullet count constraints for daily logs and summary indices.
- I authored an automated verification script to validate diary invariants and formatting rules.
- I built the complete foundational application stack using Python 3.11, FastAPI, and Uvicorn.
- I implemented asynchronous database support via SQLAlchemy 2.0 and aiosqlite with SQLite WAL mode.
- I engineered the comprehensive evidence data model spanning all 18 typed relational database tables.
- I authored Alembic migrations establishing relational schemas and SQLite FTS5 full-text search indexes.
- I developed typed repository layer in `intelx/db/repos.py` with span integrity and cryptographic audit chaining.
- I engineered the central model gateway in `intelx/models/gateway.py` supporting role-based LLM routing.
- I built the offline MockProvider with deterministic role factories and structured output self-correction.
- I engineered HttpFetchConnector with mandatory SSRF protection, IP resolution checks, and redirect guards.
- I implemented robots.txt parsing and 1-hour in-memory cache to enforce web crawling etiquette.
- I added streaming response size limits and MIME content type validation to prevent resource exhaustion.
- I built WebSearchConnector routing between Tavily, DuckDuckGo HTML scraping, and offline mock fixtures.
- I created FileConnector for local file parsing across PDF, DOCX, HTML, Markdown, CSV, and JSON formats.
- I developed the text normalization and chunking pipeline with verified absolute character offsets.
- I built the prompt injection scanner flagging risks into sidecar JSON files without mutating content.
- I authored automated pytest suites testing SSRF guards, robots enforcement, deduplication, and sanitization.
- I verified clean working tree state and synchronized all Step 4 artifacts with GitHub origin remote.

## What I Built & Did
- **Security-Hardened Connectors**:
  - I created `HttpFetchConnector` in `intelx/connectors/web.py` with multi-hop SSRF IP guards and robots.txt cache.
  - I implemented streaming body caps (`MAX_PAGE_BYTES`) and MIME content-type validation.
- **Search & File Ingestion**:
  - I authored `WebSearchConnector` in `intelx/connectors/search.py` supporting Tavily, DuckDuckGo, and mock fixtures.
  - I built `FileConnector` in `intelx/connectors/files.py` supporting PDF, DOCX, HTML, Markdown, CSV, and JSON.
- **Normalization & Prompt Injection Sanitizer**:
  - I developed `ingest_and_normalize` in `intelx/memory/normalize.py` guaranteeing `document.text[start:end] == chunk.text`.
  - I authored `IngestionSanitizer` in `intelx/connectors/sanitize.py` detecting injection signatures without mutating bytes.
- **Testing & Verification**:
  - I created test suites in `tests/test_connectors.py` (30 tests passing with 100% green pass rate).

## Bugs I Found & Fixed
- **Source-to-Document Lookup on Deduplication**:
  - *Bug*: Ingestion pipeline queried document by primary key instead of foreign key `source_id`.
  - *Fix*: Added `SourceRepo.get_document_by_source_id` to reliably resolve deduplicated parent documents.
- **Connector Policy Guard Settings Binding**:
  - *Bug*: `BaseConnector` used static default settings instead of instances passed during instantiation.
  - *Fix*: Bound dynamic settings instances directly to `self.policy_guard` in `BaseConnector.__init__`.

## Key Decisions & Architecture
- **Mandatory SSRF Protection**: Enforced DNS resolution and private/metadata IP checks across every redirect hop.
- **Non-Mutating Sanitization**: Mandated prompt injection scanner never alters retrieved text bytes.
- **Absolute Span Offsets**: Guaranteed character boundaries in chunks index directly into `document.text`.
- **Delimiter Boundaries**: Enforced wrapping external text inside `<<<EXTERNAL_DOCUMENT>>>` tags in user messages.

## Testing, Security & State
- **Automated Tests**: 30 tests passed (100% green pass rate across connectors, gateway, models, repos, and API).
- **Test Invariants**: Hardcoded Diary Gates (50 < lines < 100) and Daily Summary Gates (15 < bullets < 30).
- **Security Boundaries**: SSRF filter, robots.txt, politeness delays, and prompt injection scanner active.
- **Git Commits**: Step 4 connectors and ingestion committed and pushed to remote master branch.
