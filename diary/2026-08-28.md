# IntelX Engineering Diary — 2026-08-28

## Daily Summary
- I initialized the core IntelX project architecture and GitHub repository infrastructure.
- I formulated strict file length and bullet count constraints for daily logs and summary indices.
- I authored an automated verification script to validate diary invariants and formatting rules.
- I built the complete foundational application stack using Python 3.11, FastAPI, and Uvicorn.
- I implemented asynchronous database support via SQLAlchemy 2.0 and aiosqlite with SQLite WAL mode.
- I engineered the comprehensive evidence data model spanning all 18 typed relational database tables.
- I authored Alembic migrations establishing relational schemas and SQLite FTS5 full-text search indexes.
- I developed typed repository layer in `intelx/db/repos.py` with span integrity and cryptographic audit chaining.
- I engineered the central model gateway in `intelx/models/gateway.py` supporting role-based LLM routing.
- I built the offline MockProvider with deterministic role factories and structured output self-correction.
- I engineered HttpFetchConnector with mandatory SSRF protection, IP resolution checks, and redirect guards.
- I built WebSearchConnector and FileConnector supporting PDF, DOCX, HTML, Markdown, CSV, and JSON parsing.
- I developed the text normalization and chunking pipeline with verified absolute character offsets.
- I built the prompt injection scanner flagging risks into sidecar JSON files without mutating content.
- I developed BaseAgent architecture and AgentRegistry enforcing user-message document delimiter contracts.
- I built PlannerAgent decomposing research queries into focused plans capped at 5 atomic subquestions.
- I created ScoutAgent discovering external web sources and local knowledge base candidate matches.
- I developed RetrieverAgent executing parallel fetches with error classification and retry tolerance.
- I engineered ExtractorAgent with hard post-return verbatim span enforcement and opinion attribution rules.
- I authored automated pytest suites testing all four agents and micro-pipeline execution end-to-end.
- I verified clean working tree state and synchronized all Step 5 artifacts with GitHub origin remote.

## What I Built & Did
- **Base Agent Architecture & Delimiters**:
  - I created `BaseAgent` and `AgentRegistry` in `intelx/agents/base.py` enforcing strict security boundaries.
  - I built `format_external_document` wrapping untrusted content inside `<<<EXTERNAL_DOCUMENT>>>` delimiters.
- **First Four Typed Agents**:
  - I authored `PlannerAgent` in `intelx/agents/planner.py` with `Plan`, `SourceStrategy`, and budget distribution.
  - I built `ScoutAgent` in `intelx/agents/scout.py` integrating web search, FTS5 knowledge reuse, and policy guards.
  - I created `RetrieverAgent` in `intelx/agents/retriever.py` with error classification (`TRANSIENT` vs `LOGICAL`).
  - I engineered `ExtractorAgent` in `intelx/agents/extractor.py` enforcing exact verbatim substring matching.
- **Hard Validation Invariants**:
  - I enforced dropping ungrounded/tampered claims and recording `claim.rejected_unverifiable` audit events.
  - I required attribution regex matching for `STATEMENT_OF_OPINION` and `FORECAST` claims before persistence.
- **Testing & Verification**:
  - I authored test suites in `tests/test_agents.py` (35 tests passing with 100% green pass rate).

## Bugs I Found & Fixed
- **Retriever Session Rollback on Invalid Foreign Key**:
  - *Bug*: Dummy run ID in test caused foreign key failure which poisoned the shared test session.
  - *Fix*: Created a verified database `ResearchRun` record prior to retriever execution in tests.
- **Planner Mock Factory Schema Parity**:
  - *Bug*: Schema discrepancy between baseline gateway test model and updated agent plan definition.
  - *Fix*: Maintained backward-compatible fields in `MockProvider._generate_canned_role_data`.

## Key Decisions & Architecture
- **Verbatim Span Guarantee**: Mandated that every claim quote matches `document.text[span_start:span_end]`.
- **Attribution Enforcement**: Filtered ungrounded opinions and forecasts lacking explicit source attribution.
- **Fault-Tolerant Retrieval**: Handled candidate download failures gracefully without aborting overall research runs.
- **Prompt Security Invariant**: Prohibited placing external raw document bytes directly in system messages.

## Testing, Security & State
- **Automated Tests**: 35 tests passed (100% green pass rate across agents, connectors, gateway, and DB).
- **Test Invariants**: Hardcoded Diary Gates (50 < lines < 100) and Daily Summary Gates (15 < bullets < 30).
- **Security Boundaries**: SSRF guard, robots.txt, delimiter boundaries, and non-mutating injection scanner active.
- **Git Commits**: Step 5 first four agents committed and pushed to remote master branch.
